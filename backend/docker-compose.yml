version: "3.9"

services:
  backend:
    build: .
    container_name: clinic-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: always
    networks:
      - clinic-network

networks:
  clinic-network:
